    // java log sheet.


// Arrays ;

.length() = 'finding the lenght of an array (elements)';
.indexof(e.g. n , ba) = 'find the index number of an array ';
.slice () = 'it cuts down characters of an array';
.replace() = 'it replaces characters';
.join() = 'it adds what ever u place in the brackts between your arrays';
.pop () = ' it removes the last element from an arrat';
.push () = 'it apppends (adds to the last element to an array)';
.lenght = "  " = ' same as ".push" ';
.shift () = 'it removes the first element from an array';
.unshift() = 'it adds to the first of an array';
.reverse () = 'it reverse arrays order';

//Looping;

for ( let i = 0; i < ['any number']; i++);